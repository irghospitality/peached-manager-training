/* ============================================================
   COMPLETION EMAIL  —  paste your Google Apps Script Web App URL
   below so completion alerts go to Shavonne and Heather.

   (Setup steps are in the README. Until you paste a real URL,
   completions still show in Manager mode — just no email is sent.)
   ============================================================ */

const NOTIFY_URL = "PASTE_YOUR_APPS_SCRIPT_WEB_APP_URL";
